# API Reference

This section provides a detailed reference for the Lilac backend API. The API is organized by domain, with each section documenting the available endpoints, their parameters, and the expected responses.

### Authentication

Most API endpoints require a JWT for authentication. However, CLI operations to the backend require an API key.